type SetSize = {
  setWidth: number;
  setHeight: number;
};

type ItemInfo = {
  name: string;
  price: string;
  image: Array<string>;
  date: string;
  code_name: string;
  key: number;
};

export type { SetSize, ItemInfo };
